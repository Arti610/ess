import apiService from "../../../config/apiService";

export default{
    // updateUser: (id, payload, option) => apiService.post(`updateuser/${id}`, payload, option),
    updateUser: (id, payload, option) => console.log('id',id)
  
}